/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Flowlane API
 * Backend contract expected by the Flowlane frontend (`frontend/`), which
 * currently talks to a mock implementation of this exact contract in
 * `frontend/src/api/client.ts` (persisted to `localStorage`). This file
 * documents that mock precisely so a real backend can be dropped in with no
 * frontend changes — see `frontend/src/api/` and `_docs/specs.md` §6-9.
 *
 * ### Authentication
 *
 * **None of the endpoints below require authentication.** The frontend
 * never sends credentials or an `Authorization` header, and every board it
 * creates is attributed to a single hardcoded `ownerId` ("local-user").
 * This matches `_docs/specs.md` §15, which explicitly defers auth to
 * Phase 2/6: "For the first version, authentication can be skipped and
 * added in Phase 2." When that phase happens, `ownerId` should switch from
 * a hardcoded value to the authenticated user's id, and the endpoints below
 * will need a bearer-token (or session) requirement added — until then this
 * spec has no `securitySchemes` and every operation is effectively public.
 *
 * ### Error shape
 *
 * Every error response uses the same envelope (`_docs/specs.md` §14):
 *
 * ```json
 * { "error": { "code": "VALIDATION_ERROR", "message": "Task title is required" } }
 * ```
 *
 * The mock only ever produces two codes, reflected throughout this spec:
 * - `VALIDATION_ERROR` (400) — a request body failed the rules in
 *   `frontend/src/utils/validation.ts` (mirrored in each schema below).
 * - `NOT_FOUND` (404) — a path parameter (`boardId`, `columnId`, `taskId`)
 *   doesn't reference an existing record.
 *
 * A real backend will likely add more codes (e.g. `CONFLICT`, `UNAUTHORIZED`,
 * `INTERNAL_ERROR`) but should keep the same `{ error: { code, message } }`
 * envelope, since `frontend/src/api/apiError.ts` only reads `code`/`message`.
 *
 * ### Ordering
 *
 * `position` is a zero-based integer maintained by the server, never chosen
 * freely by the client — see `_docs/specs.md` §5 ("Use a numeric position
 * rather than relying on task IDs"). Clients influence it only through
 * the dedicated reorder/move endpoints (`PATCH /boards/{boardId}/columns/reorder`,
 * `PATCH /tasks/{taskId}/move`); it is never a writable field on create/update
 * bodies, and the server is responsible for keeping positions contiguous
 * (0, 1, 2, …) within a board's columns and within a column's tasks after
 * any insert, delete, or reorder.
 *
 * OpenAPI spec version: 0.1.0
 */
import type {
  CreateTaskInput,
  Error,
  MoveTaskInput,
  NotFoundResponse,
  Task,
  UpdateTaskInput,
  ValidationErrorResponse
} from '../../model';


export type createTaskResponse201 = {
  data: Task
  status: 201
}

export type createTaskResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type createTaskResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type createTaskResponseSuccess = (createTaskResponse201) & {
  headers: Headers;
};
export type createTaskResponseError = (createTaskResponse400 | createTaskResponse404) & {
  headers: Headers;
};

export type createTaskResponse = (createTaskResponseSuccess | createTaskResponseError)

export const getCreateTaskUrl = (columnId: string,) => {




  return `/api/columns/${columnId}/tasks`
}

/**
 * Appends a new task at the end of the column (`position` = current
 * task count in that column). `priority` defaults to `MEDIUM` when
 * omitted.
 * @summary Create a task in a column
 */
export const createTask = async (columnId: string,
    createTaskInput: CreateTaskInput, options?: RequestInit): Promise<createTaskResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getCreateTaskUrl(columnId),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(createTaskInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: createTaskResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as createTaskResponse
}


export type getTaskResponse200 = {
  data: Task
  status: 200
}

export type getTaskResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type getTaskResponseSuccess = (getTaskResponse200) & {
  headers: Headers;
};
export type getTaskResponseError = (getTaskResponse404) & {
  headers: Headers;
};

export type getTaskResponse = (getTaskResponseSuccess | getTaskResponseError)

export const getGetTaskUrl = (taskId: string,) => {




  return `/api/tasks/${taskId}`
}

/**
 * Exposed by the API client (`frontend/src/api/client.ts`) for
 * completeness with `_docs/specs.md` §8, though the board page
 * currently reads task data from the already-fetched
 * `GET /boards/{boardId}` payload rather than calling this directly.
 * @summary Get a single task
 */
export const getTask = async (taskId: string, options?: RequestInit): Promise<getTaskResponse> => {

  const res = await fetch(getGetTaskUrl(taskId),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: getTaskResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as getTaskResponse
}


export type updateTaskResponse200 = {
  data: Task
  status: 200
}

export type updateTaskResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type updateTaskResponse404 = {
  data: Error
  status: 404
}

export type updateTaskResponseSuccess = (updateTaskResponse200) & {
  headers: Headers;
};
export type updateTaskResponseError = (updateTaskResponse400 | updateTaskResponse404) & {
  headers: Headers;
};

export type updateTaskResponse = (updateTaskResponseSuccess | updateTaskResponseError)

export const getUpdateTaskUrl = (taskId: string,) => {




  return `/api/tasks/${taskId}`
}

/**
 * All fields are optional — only supplied fields are changed
 * (`frontend/src/hooks/useTaskMutations.ts` `useUpdateTaskMutation`,
 * used by the task edit modal). Setting `columnId` here moves the task
 * to the end of that column; to control the exact drop position (as
 * drag-and-drop needs to), use `PATCH /tasks/{taskId}/move` instead.
 * @summary Edit a task
 */
export const updateTask = async (taskId: string,
    updateTaskInput: UpdateTaskInput, options?: RequestInit): Promise<updateTaskResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getUpdateTaskUrl(taskId),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(updateTaskInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: updateTaskResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as updateTaskResponse
}


export type deleteTaskResponse204 = {
  data: void
  status: 204
}

export type deleteTaskResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type deleteTaskResponseSuccess = (deleteTaskResponse204) & {
  headers: Headers;
};
export type deleteTaskResponseError = (deleteTaskResponse404) & {
  headers: Headers;
};

export type deleteTaskResponse = (deleteTaskResponseSuccess | deleteTaskResponseError)

export const getDeleteTaskUrl = (taskId: string,) => {




  return `/api/tasks/${taskId}`
}

/**
 * Reindexes the remaining tasks in its column so `position` stays contiguous.
 * @summary Delete a task
 */
export const deleteTask = async (taskId: string, options?: RequestInit): Promise<deleteTaskResponse> => {

  const res = await fetch(getDeleteTaskUrl(taskId),
  {
    ...options,
    method: 'DELETE'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: deleteTaskResponse['data'] = body ? JSON.parse(body) : undefined
  return { data, status: res.status, headers: res.headers } as deleteTaskResponse
}


export type moveTaskResponse200 = {
  data: Task
  status: 200
}

export type moveTaskResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type moveTaskResponse404 = {
  data: Error
  status: 404
}

export type moveTaskResponseSuccess = (moveTaskResponse200) & {
  headers: Headers;
};
export type moveTaskResponseError = (moveTaskResponse400 | moveTaskResponse404) & {
  headers: Headers;
};

export type moveTaskResponse = (moveTaskResponseSuccess | moveTaskResponseError)

export const getMoveTaskUrl = (taskId: string,) => {




  return `/api/tasks/${taskId}/move`
}

/**
 * The dedicated drag-and-drop endpoint (`_docs/specs.md` §8,
 * `frontend/src/hooks/useTaskMutations.ts` `useMoveTaskMutation`) for
 * both "reorder within a column" and "move to another column" —
 * `columnId` may equal the task's current column. `position` is the
 * desired **zero-based index within the destination column**
 * (in the destination's post-move task order, not a global position);
 * out-of-range values are clamped by the server to
 * `[0, destinationTaskCount]`. The server reindexes both the source
 * and destination columns' tasks afterward.
 *
 * Per `_docs/specs.md` §11, the frontend applies this change to its UI
 * optimistically before this request resolves, and rolls back if it
 * fails — so this endpoint should fail fast and predictably rather
 * than partially apply a move.
 * @summary Move (and/or reorder) a task
 */
export const moveTask = async (taskId: string,
    moveTaskInput: MoveTaskInput, options?: RequestInit): Promise<moveTaskResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getMoveTaskUrl(taskId),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(moveTaskInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: moveTaskResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as moveTaskResponse
}



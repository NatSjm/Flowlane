/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Flowlane API
 * Backend contract expected by the Flowlane frontend (`frontend/`), which
 * currently talks to a mock implementation of this exact contract in
 * `frontend/src/api/client.ts` (persisted to `localStorage`). This file
 * documents that mock precisely so a real backend can be dropped in with no
 * frontend changes — see `frontend/src/api/` and `_docs/specs.md` §6-9.
 *
 * ### Authentication
 *
 * **None of the endpoints below require authentication.** The frontend
 * never sends credentials or an `Authorization` header, and every board it
 * creates is attributed to a single hardcoded `ownerId` ("local-user").
 * This matches `_docs/specs.md` §15, which explicitly defers auth to
 * Phase 2/6: "For the first version, authentication can be skipped and
 * added in Phase 2." When that phase happens, `ownerId` should switch from
 * a hardcoded value to the authenticated user's id, and the endpoints below
 * will need a bearer-token (or session) requirement added — until then this
 * spec has no `securitySchemes` and every operation is effectively public.
 *
 * ### Error shape
 *
 * Every error response uses the same envelope (`_docs/specs.md` §14):
 *
 * ```json
 * { "error": { "code": "VALIDATION_ERROR", "message": "Task title is required" } }
 * ```
 *
 * The mock only ever produces two codes, reflected throughout this spec:
 * - `VALIDATION_ERROR` (400) — a request body failed the rules in
 *   `frontend/src/utils/validation.ts` (mirrored in each schema below).
 * - `NOT_FOUND` (404) — a path parameter (`boardId`, `columnId`, `taskId`)
 *   doesn't reference an existing record.
 *
 * A real backend will likely add more codes (e.g. `CONFLICT`, `UNAUTHORIZED`,
 * `INTERNAL_ERROR`) but should keep the same `{ error: { code, message } }`
 * envelope, since `frontend/src/api/apiError.ts` only reads `code`/`message`.
 *
 * ### Ordering
 *
 * `position` is a zero-based integer maintained by the server, never chosen
 * freely by the client — see `_docs/specs.md` §5 ("Use a numeric position
 * rather than relying on task IDs"). Clients influence it only through
 * the dedicated reorder/move endpoints (`PATCH /boards/{boardId}/columns/reorder`,
 * `PATCH /tasks/{taskId}/move`); it is never a writable field on create/update
 * bodies, and the server is responsible for keeping positions contiguous
 * (0, 1, 2, …) within a board's columns and within a column's tasks after
 * any insert, delete, or reorder.
 *
 * OpenAPI spec version: 0.1.0
 */
import type {
  Board,
  BoardDetail,
  BoardSummary,
  CreateBoardInput,
  NotFoundResponse,
  UpdateBoardInput,
  ValidationErrorResponse
} from '../../model';


export type listBoardsResponse200 = {
  data: BoardSummary[]
  status: 200
}

export type listBoardsResponseSuccess = (listBoardsResponse200) & {
  headers: Headers;
};
;

export type listBoardsResponse = (listBoardsResponseSuccess)

export const getListBoardsUrl = () => {




  return `/api/boards`
}

/**
 * Powers the `/` dashboard. Returns one summary per board, each with
 * aggregate column/task counts (`frontend/src/hooks/useBoards.ts`
 * `useBoardsQuery`), sorted oldest-created first.
 * @summary List boards
 */
export const listBoards = async ( options?: RequestInit): Promise<listBoardsResponse> => {

  const res = await fetch(getListBoardsUrl(),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: listBoardsResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as listBoardsResponse
}


export type createBoardResponse201 = {
  data: Board
  status: 201
}

export type createBoardResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type createBoardResponseSuccess = (createBoardResponse201) & {
  headers: Headers;
};
export type createBoardResponseError = (createBoardResponse400) & {
  headers: Headers;
};

export type createBoardResponse = (createBoardResponseSuccess | createBoardResponseError)

export const getCreateBoardUrl = () => {




  return `/api/boards`
}

/**
 * Creates a board and, as a side effect, seeds it with the three
 * default columns **Todo**, **In Progress**, **Done** at positions
 * 0, 1, 2 (`_docs/specs.md` §2). The frontend relies on this: it does
 * not make a separate call to create default columns.
 * @summary Create a board
 */
export const createBoard = async (createBoardInput: CreateBoardInput, options?: RequestInit): Promise<createBoardResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getCreateBoardUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(createBoardInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: createBoardResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as createBoardResponse
}


export type getBoardResponse200 = {
  data: BoardDetail
  status: 200
}

export type getBoardResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type getBoardResponseSuccess = (getBoardResponse200) & {
  headers: Headers;
};
export type getBoardResponseError = (getBoardResponse404) & {
  headers: Headers;
};

export type getBoardResponse = (getBoardResponseSuccess | getBoardResponseError)

export const getGetBoardUrl = (boardId: string,) => {




  return `/api/boards/${boardId}`
}

/**
 * Powers the `/boards/:boardId` board page
 * (`frontend/src/hooks/useBoard.ts`). Returns the board with its
 * columns, each carrying its tasks — both sorted by `position`
 * ascending. This is the single source of truth the frontend caches
 * and optimistically patches for every column/task mutation, so its
 * shape (nested columns → nested tasks) must stay exactly as below.
 * @summary Get a board's full detail
 */
export const getBoard = async (boardId: string, options?: RequestInit): Promise<getBoardResponse> => {

  const res = await fetch(getGetBoardUrl(boardId),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: getBoardResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as getBoardResponse
}


export type updateBoardResponse200 = {
  data: Board
  status: 200
}

export type updateBoardResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type updateBoardResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type updateBoardResponseSuccess = (updateBoardResponse200) & {
  headers: Headers;
};
export type updateBoardResponseError = (updateBoardResponse400 | updateBoardResponse404) & {
  headers: Headers;
};

export type updateBoardResponse = (updateBoardResponseSuccess | updateBoardResponseError)

export const getUpdateBoardUrl = (boardId: string,) => {




  return `/api/boards/${boardId}`
}

/**
 * Only supports renaming today — `frontend/src/hooks/useBoards.ts` `useRenameBoardMutation`.
 * @summary Rename a board
 */
export const updateBoard = async (boardId: string,
    updateBoardInput: UpdateBoardInput, options?: RequestInit): Promise<updateBoardResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getUpdateBoardUrl(boardId),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(updateBoardInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: updateBoardResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as updateBoardResponse
}


export type deleteBoardResponse204 = {
  data: void
  status: 204
}

export type deleteBoardResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type deleteBoardResponseSuccess = (deleteBoardResponse204) & {
  headers: Headers;
};
export type deleteBoardResponseError = (deleteBoardResponse404) & {
  headers: Headers;
};

export type deleteBoardResponse = (deleteBoardResponseSuccess | deleteBoardResponseError)

export const getDeleteBoardUrl = (boardId: string,) => {




  return `/api/boards/${boardId}`
}

/**
 * Cascades — deletes every column and task belonging to the board.
 * @summary Delete a board
 */
export const deleteBoard = async (boardId: string, options?: RequestInit): Promise<deleteBoardResponse> => {

  const res = await fetch(getDeleteBoardUrl(boardId),
  {
    ...options,
    method: 'DELETE'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: deleteBoardResponse['data'] = body ? JSON.parse(body) : undefined
  return { data, status: res.status, headers: res.headers } as deleteBoardResponse
}



/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Flowlane API
 * Backend contract expected by the Flowlane frontend (`frontend/`), which
 * currently talks to a mock implementation of this exact contract in
 * `frontend/src/api/client.ts` (persisted to `localStorage`). This file
 * documents that mock precisely so a real backend can be dropped in with no
 * frontend changes — see `frontend/src/api/` and `_docs/specs.md` §6-9.
 *
 * ### Authentication
 *
 * **None of the endpoints below require authentication.** The frontend
 * never sends credentials or an `Authorization` header, and every board it
 * creates is attributed to a single hardcoded `ownerId` ("local-user").
 * This matches `_docs/specs.md` §15, which explicitly defers auth to
 * Phase 2/6: "For the first version, authentication can be skipped and
 * added in Phase 2." When that phase happens, `ownerId` should switch from
 * a hardcoded value to the authenticated user's id, and the endpoints below
 * will need a bearer-token (or session) requirement added — until then this
 * spec has no `securitySchemes` and every operation is effectively public.
 *
 * ### Error shape
 *
 * Every error response uses the same envelope (`_docs/specs.md` §14):
 *
 * ```json
 * { "error": { "code": "VALIDATION_ERROR", "message": "Task title is required" } }
 * ```
 *
 * The mock only ever produces two codes, reflected throughout this spec:
 * - `VALIDATION_ERROR` (400) — a request body failed the rules in
 *   `frontend/src/utils/validation.ts` (mirrored in each schema below).
 * - `NOT_FOUND` (404) — a path parameter (`boardId`, `columnId`, `taskId`)
 *   doesn't reference an existing record.
 *
 * A real backend will likely add more codes (e.g. `CONFLICT`, `UNAUTHORIZED`,
 * `INTERNAL_ERROR`) but should keep the same `{ error: { code, message } }`
 * envelope, since `frontend/src/api/apiError.ts` only reads `code`/`message`.
 *
 * ### Ordering
 *
 * `position` is a zero-based integer maintained by the server, never chosen
 * freely by the client — see `_docs/specs.md` §5 ("Use a numeric position
 * rather than relying on task IDs"). Clients influence it only through
 * the dedicated reorder/move endpoints (`PATCH /boards/{boardId}/columns/reorder`,
 * `PATCH /tasks/{taskId}/move`); it is never a writable field on create/update
 * bodies, and the server is responsible for keeping positions contiguous
 * (0, 1, 2, …) within a board's columns and within a column's tasks after
 * any insert, delete, or reorder.
 *
 * OpenAPI spec version: 0.1.0
 */
import type {
  Column,
  CreateColumnInput,
  Error,
  NotFoundResponse,
  ReorderColumnsInput,
  UpdateColumnInput,
  ValidationErrorResponse
} from '../../model';


export type createColumnResponse201 = {
  data: Column
  status: 201
}

export type createColumnResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type createColumnResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type createColumnResponseSuccess = (createColumnResponse201) & {
  headers: Headers;
};
export type createColumnResponseError = (createColumnResponse400 | createColumnResponse404) & {
  headers: Headers;
};

export type createColumnResponse = (createColumnResponseSuccess | createColumnResponseError)

export const getCreateColumnUrl = (boardId: string,) => {




  return `/api/boards/${boardId}/columns`
}

/**
 * Appends a new column at the end of the board (`position` = current
 * column count). Used both for the "+ Add column" control and — via
 * the same input shape — the three default columns a new board is
 * seeded with.
 * @summary Create a column on a board
 */
export const createColumn = async (boardId: string,
    createColumnInput: CreateColumnInput, options?: RequestInit): Promise<createColumnResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getCreateColumnUrl(boardId),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(createColumnInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: createColumnResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as createColumnResponse
}


export type reorderColumnsResponse200 = {
  data: Column[]
  status: 200
}

export type reorderColumnsResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type reorderColumnsResponse404 = {
  data: Error
  status: 404
}

export type reorderColumnsResponseSuccess = (reorderColumnsResponse200) & {
  headers: Headers;
};
export type reorderColumnsResponseError = (reorderColumnsResponse400 | reorderColumnsResponse404) & {
  headers: Headers;
};

export type reorderColumnsResponse = (reorderColumnsResponseSuccess | reorderColumnsResponseError)

export const getReorderColumnsUrl = (boardId: string,) => {




  return `/api/boards/${boardId}/columns/reorder`
}

/**
 * Backs column drag-and-drop (`frontend/src/hooks/useColumnMutations.ts`
 * `useReorderColumnsMutation`). `columnIds` must list **every** column
 * currently on the board, in the desired new order; the server sets
 * each column's `position` to its index in that array (0-based).
 * @summary Reorder a board's columns
 */
export const reorderColumns = async (boardId: string,
    reorderColumnsInput: ReorderColumnsInput, options?: RequestInit): Promise<reorderColumnsResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getReorderColumnsUrl(boardId),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(reorderColumnsInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: reorderColumnsResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as reorderColumnsResponse
}


export type updateColumnResponse200 = {
  data: Column
  status: 200
}

export type updateColumnResponse400 = {
  data: ValidationErrorResponse
  status: 400
}

export type updateColumnResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type updateColumnResponseSuccess = (updateColumnResponse200) & {
  headers: Headers;
};
export type updateColumnResponseError = (updateColumnResponse400 | updateColumnResponse404) & {
  headers: Headers;
};

export type updateColumnResponse = (updateColumnResponseSuccess | updateColumnResponseError)

export const getUpdateColumnUrl = (columnId: string,) => {




  return `/api/columns/${columnId}`
}

/**
 * Only supports renaming today — `frontend/src/hooks/useColumnMutations.ts` `useRenameColumnMutation`.
 * @summary Rename a column
 */
export const updateColumn = async (columnId: string,
    updateColumnInput: UpdateColumnInput, options?: RequestInit): Promise<updateColumnResponse> => {

    const getHeaders = (h?: NonNullable<RequestInit['headers']>): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(h as Iterable<Iterable<string>>, (entry) => Array.from(entry) as [string, string]),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
const res = await fetch(getUpdateColumnUrl(columnId),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...getHeaders(options?.headers) },
    body: JSON.stringify(updateColumnInput)
  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: updateColumnResponse['data'] = body ? JSON.parse(body) : {}
  return { data, status: res.status, headers: res.headers } as updateColumnResponse
}


export type deleteColumnResponse204 = {
  data: void
  status: 204
}

export type deleteColumnResponse404 = {
  data: NotFoundResponse
  status: 404
}

export type deleteColumnResponseSuccess = (deleteColumnResponse204) & {
  headers: Headers;
};
export type deleteColumnResponseError = (deleteColumnResponse404) & {
  headers: Headers;
};

export type deleteColumnResponse = (deleteColumnResponseSuccess | deleteColumnResponseError)

export const getDeleteColumnUrl = (columnId: string,) => {




  return `/api/columns/${columnId}`
}

/**
 * Cascades — deletes every task in the column — then reindexes the
 * board's remaining columns so `position` stays contiguous (0, 1, 2, …).
 * @summary Delete a column
 */
export const deleteColumn = async (columnId: string, options?: RequestInit): Promise<deleteColumnResponse> => {

  const res = await fetch(getDeleteColumnUrl(columnId),
  {
    ...options,
    method: 'DELETE'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: deleteColumnResponse['data'] = body ? JSON.parse(body) : undefined
  return { data, status: res.status, headers: res.headers } as deleteColumnResponse
}


